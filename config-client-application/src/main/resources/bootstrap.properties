# 服务器端口。默认为 8080
spring.application.name=user-application
# Spring Cloud Config Server 的地址。
spring.cloud.config.uri=http://127.0.0.1:8888
# 读取的配置文件的名字，默认为 ${spring.application.name}。通过该默认值，实现了 Spring Cloud Config Client 的默认约定的读取。
spring.cloud.config.name=user-application

